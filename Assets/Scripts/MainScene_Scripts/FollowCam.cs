using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FollowCam : MonoBehaviour
{
    [SerializeField] float DistanceHeight;
    [SerializeField] float DistanceWidt;
    Camera cam;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
